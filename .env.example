# ---- App ----
# WhatsApp admin number (international format, no +, no spaces). Configurable in admin panel too.
NEXT_PUBLIC_WHATSAPP_NUMBER=919999999999

# ---- Database (Prisma + SQLite for the demo) ----
DATABASE_URL="file:./dev.db"

# ---- Admin auth ----
# Used to sign the admin session JWT. Change in production.
ADMIN_JWT_SECRET="change-me-in-production-super-secret-key"
# Default admin credentials (seeded). Change after first login.
ADMIN_EMAIL="admin@chatpata.com"
ADMIN_PASSWORD="achaar123"
