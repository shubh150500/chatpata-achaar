# Place your logo here

Drop your logo file named **`logo.png`** (or `logo.svg`) into this `/public`
folder. It will automatically replace the "CA" placeholder emblem across the
site (hero, navbar, footer, admin).

Recommended: a transparent PNG, ~512×512px.
